SET(CMAKE_CXX_COMPILER /usr/bin/g++-11)
SET(CMAKE_C_COMPILER /usr/bin/gcc-11)
SET(CMAKE_CXX_STANDARD 20)
SET(CMAKE_CXX_STANDARD_REQUIRED True) 

IF(CMAKE_BUILD_TYPE STREQUAL "Debug")
SET(CMAKE_CXX_FLAGS "-g -Wall -Wextra")
ELSEIF(CMAKE_BUILD_TYPE STREQUAL "Release")
SET(CMAKE_CXX_FLAGS "-O2")
ENDIF()

#SET(CMAKE_CXX_FLAGS "-g -Wall -Wextra")
SET(CMAKE_LINKER_FLAGS "-pthread")

INCLUDE_DIRECTORIES(${BLAZE_INCLUDE_DIRS}) 
INCLUDE_DIRECTORIES(${BOOST_INCLUDEDIR})
INCLUDE_DIRECTORIES(${NLOHMANN_JSON_INCL_DIR}) 
INCLUDE_DIRECTORIES(${TORCH_INCLUDE_DIRS})
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/include) 
INCLUDE_DIRECTORIES(${GYMFCPP_INCLUDE_DIRS}) 

LINK_DIRECTORIES(${CMAKE_INSTALL_PREFIX})
LINK_DIRECTORIES(${Boost_LIBRARY_DIRS})
LINK_DIRECTORIES(${GYMFCPP_LIB_DIRS}) 
LINK_DIRECTORIES(${PYTHON_CONFIG_LIBS_PATH}) 

ADD_SUBDIRECTORY(rl)
ADD_SUBDIRECTORY(ml)
ADD_SUBDIRECTORY(path_tracking)

